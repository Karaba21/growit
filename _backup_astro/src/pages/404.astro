---
import MainLayout from '../layouts/MainLayout.astro';
import { Button } from '../components/ui/Button';
---

<MainLayout title="Página no encontrada - Growit" description="La página que buscas no existe">
  <div class="min-h-screen bg-gradient-to-b from-white to-primary-50 flex items-center justify-center px-4">
    <div class="max-w-2xl w-full text-center">
      {/* 404 Illustration */}
      <div class="mb-8">
        <h1 class="text-9xl font-bold text-primary-600 mb-4">404</h1>
        <div class="text-6xl mb-4">🌱</div>
      </div>
      
      {/* Error Message */}
      <h2 class="text-3xl md:text-4xl font-bold text-gray-900 mb-4">
        ¡Oops! Página no encontrada
      </h2>
      
      <p class="text-lg text-gray-600 mb-8 max-w-md mx-auto">
        Parece que esta página no existe o ha sido movida. 
        No te preocupes, te ayudaremos a encontrar lo que buscas.
      </p>
      
      {/* Action Buttons */}
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center">
        <a href="/">
          <Button size="lg" client:load>
            Volver al inicio
          </Button>
        </a>
        <a href="/catalogo">
          <Button variant="outline" size="lg" client:load>
            Ver catálogo
          </Button>
        </a>
      </div>
      
      {/* Helpful Links */}
      <div class="mt-12 pt-8 border-t border-gray-200">
        <p class="text-sm text-gray-500 mb-4">Enlaces útiles:</p>
        <div class="flex flex-wrap gap-4 justify-center text-sm">
          <a href="/" class="text-primary-600 hover:text-primary-700 hover:underline">
            Inicio
          </a>
          <a href="/catalogo" class="text-primary-600 hover:text-primary-700 hover:underline">
            Catálogo
          </a>
        </div>
      </div>
    </div>
  </div>
</MainLayout>
